
def change_strings(data):
    """
    Change strings into random characters
    """

    # Find all the strings
    # Rename them to random characters

def remove_comments(data):
    """
    Remove comments in any language
    """

    # Detect language from markdown parser
    # Search the comment syntax
    # Remove the comments


def ip_obfuscate(ip):
    """
    Obfuscate an IPv4 and IPv6 address by converting it to decimal, hex, 
    octal, or a combination of the three.
    """

    # Detect IP version
    # Convert decimal to hexadecimal (separted by the octets)


def gen_random_var(svars, lang):
    """
    Returns a randomly named variable.
    """

    # Detect variables
    # Rename variables randomly

